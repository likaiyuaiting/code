#include<stdio.h>
#include<stdlib.h>

int main(void)
{
	int x, y, n;
	printf("请输入N值：");
	scanf("%d",&n);
	for (y = 1; y <2 * n; y++){
	for (x = 0; x <2 * n;x++)
	if (y == x || y == -x + 2 * n )
		printf("*");
	else  printf(" ");
	printf("\n");
	}
	system("pause");
	return 0;
}
